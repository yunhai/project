<section class="panel panel-default">
    <header class="panel-heading font-bold">
        {{ __('2101', 'Thông tin cơ bản', 'global') }}
        <ul class="nav nav-pills pull-right">
            <li>
                <a href="#" class="panel-toggle text-muted">
                    <i class="fa fa-caret-down text-active"></i>
                    <i class="fa fa-caret-up text"></i>
                </a>
            </li>
        </ul>
    </header>
    <div class="panel-body">
        <input type='hidden' name='{{ alias }}[id]' value='{{ attribute(target, alias).id }}' />

        <table class="table table-bordered">
            <tr>
                <th class='col-sm-3 table-label'>
                    <label for="account">{{ __('2001', 'Account') }}</label>
                </th>
                <td>
                    {{ attribute(target, alias).account }}
                </td>
            </tr>
            <tr>
                <th class='col-sm-3 table-label'>
                    <label for="email">{{ __('2001', 'Email') }}</label>
                </th>
                <td>
                    {{ attribute(target, alias).email }}
                </td>
            </tr>
            <tr>
                <th class='col-sm-3 table-label'>
                    <label for="current-password">Current password</label>
                    <span class="text-danger pull-right">
                        {{ __('2001', 'Bắt buộc', 'global') }}
                    </span>
                </th>
                <td>
                    <input class='form-control' name='{{ alias }}[current-password]' type='password' id='current-password' />
                    {% if error is defined %}
                        {{ error|error(alias, 'current-password') }}
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class='col-sm-3 table-label'>
                    <label for="new-password">New password</label>
                    <span class="text-danger pull-right">
                        {{ __('2001', 'Bắt buộc', 'global') }}
                    </span>
                </th>
                <td>
                    <input class='form-control' name='{{ alias }}[password]' type='password' id='new-password' />
                    {% if error is defined %}
                        {{ error|error(alias, 'password') }}
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class='col-sm-3 table-label'>
                    <label for="slug">Confirm Password</label>
                    <span class="text-danger pull-right">
                        {{ __('2001', 'Bắt buộc', 'global') }}
                    </span>
                </th>
                <td>
                    <input class='form-control' name='{{ alias }}[confirm-password]' type='password' id='confirm-password' />
                    {% if error is defined %}
                        {{ error|error(alias, 'confirm-password') }}
                    {% endif %}
                </td>
            </tr>
        </table>
    </div>
</section>